# Next.js
.next/
out/

# Dependencies
node_modules/

# Build artifacts / archives
*.tsbuildinfo
*.tar.gz
*.tar
*.zip

# Environment + deploy
.vercel
.env*.local
.env.local

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Local data dumps
supabase.tar.gz
*.dump
*.sql.gz

# Coverage / logs
coverage/
*.log

# Test artifacts
test-results/
playwright-report/
playwright/.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Workflow agent worktrees
.claude/worktrees/
scripts/ethno_seed.json
scripts/iucn_updates.json
scripts/pub_updates.json

# Supabase CLI binary (downloaded to repo root; do not commit to this PUBLIC repo)
/supabase

# Local working notes — strategy/commercial review material that must stay out
# of the public repo (kept on the founder's machine only).
docs/from-chatgpt/
