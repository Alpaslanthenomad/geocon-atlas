import AssertFact from "../../../components/geocon/AssertFact";

export const metadata = {
  title: "Assert a fact — GEOCON",
  description: "Assert one real, evidence-backed fact about a threatened plant and mint a money-blind, citable Provenance Receipt with your name on it. Move the atlas from 0.",
};

export default function ContributePage() {
  return <AssertFact />;
}
