import EndemiconRoute from "../../../components/geocon/EndemiconRoute";

export const metadata = {
  title: "EndemiCon — endemik geofitlerin korunması | GEOCON",
  description: "Yalnız tek bir ülkede yetişen tehdit altındaki endemik geofitler ve korunma boşlukları.",
};

export default function EndemiconPage() {
  return <EndemiconRoute />;
}
