import OutcomeTimelineRoute from "../../../../components/geocon/OutcomeTimelineRoute";

export const metadata = {
  title: "Outcome timeline — GEOCON",
  description: "Cross-species conservation + commercialization outcome timeline, grouped by month and verification tier.",
};

export default function OutcomeTimelinePage() {
  return <OutcomeTimelineRoute />;
}
