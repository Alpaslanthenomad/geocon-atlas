import VennExchangeRoute from "../../components/exchange/VennExchangeRoute";

export const metadata = {
  title: "Venn Exchange — BEE",
  description:
    "The cross-vertical commerce home where verified conservation value outputs meet investors, VENN ventures, and industry. The conservation atlases stay pure — money never crosses into them.",
};

export default function ExchangePage() {
  return <VennExchangeRoute />;
}
